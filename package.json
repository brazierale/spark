{
  "name": "spark",
  "version": "0.1.0",
  "scripts": {
    "client": "cd client && yarn start",
    "server": "cd api && nodemon express.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.2",
    "tedious": "^2.6.2"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
